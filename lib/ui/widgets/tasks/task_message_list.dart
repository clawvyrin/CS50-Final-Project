import 'package:flutter/material.dart';

class TaskMessageList extends StatelessWidget {
  const TaskMessageList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
