import 'package:flutter/material.dart';

class ProjectGantChart extends StatelessWidget {
  const ProjectGantChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
